# 对话记录 — 应用 OpenBMB PR #10 `_init_rope` 修复（兼容 transformers 5.x）

会话：2026-05-08 12:18

## 1. 用户提问 / Agent 行动

- **用户**：「before go iter-4, pls help to patch modeling_minicpm_sala.py as
  the below public official announcement mentioned」（HF 讨论 #10）。
- **Agent**：抓取 HF 讨论 #10 与 commit `f28de5e4` 原始 diff。确认改动范围：
  两个 `_init_rope()`、把 `None`/`"default"` 都视作不缩放、同时接受
  `"type"` 与 `"rope_type"` 键、把 `rope_scaling` 提为局部变量。
- **Agent**：定位模型文件。它**不在**本仓库——通过 HuggingFace
  `trust_remote_code=True` 下载，仅存在于模型目录中。我们已有量化前
  内存 hack（`_install_gptqmodel_minicpm_rope_patch`）覆盖 GPTQ 路径，
  但 sglang 推理路径没有保护。
- **Agent**：决定在 `preprocess_model.py` 里**就地**对模型目录应用上游补丁，
  以同时覆盖量化期与运行时。新增
  `_patch_modeling_init_rope_inplace(model_dir, label)`，使用标记位做幂等
  保护，并在 `gptq`、`nvfp4`、`copy` 三种模式 finalize 后各调用一次。
- **Agent**：写了一个往返测试——从上游 post-fix 文件按正确顺序反向替换出
  pre-fix 文件，跑 patcher，和上游 post-fix 字节比对。结果：完全一致，
  且对已修补文件幂等。
- **Agent**：撰写中英双语文档
  `CHANGE_0152_init_rope_transformers5_compat.{en,zh}.md`。

## 2. 产出

- 在 `benchmark/soar/demo_sala/preprocess_model.py` 中新增
  `_patch_modeling_init_rope_inplace`，三种模式中均接入。
- 本地往返补丁测试通过（与上游字节一致、幂等）。
- CHANGE_0152 中英文档已提交。

## 3. 待跟进

- iter-4 fcloud 运行将是首次端到端验证。期望日志看到
  `[preprocess][init-rope-patch] ... patched ... (replaced 2 _init_rope headers,
  2 else-branches)`。
- 若有任何代码路径绕过 `preprocess_model.py`（我方流程不会），可能还需要修补
  `~/.cache/huggingface/modules/transformers_modules/...` 下的 HF cache 拷贝。
  等真正遇到再处理。

## 4. 交叉引用

- 文档：[docs/soar_2026_changes/CHANGE_0152_init_rope_transformers5_compat.en.md](../CHANGE_0152_init_rope_transformers5_compat.en.md)、[docs/soar_2026_changes/CHANGE_0152_init_rope_transformers5_compat.zh.md](../CHANGE_0152_init_rope_transformers5_compat.zh.md)
- 涉及文件：[benchmark/soar/demo_sala/preprocess_model.py](../../../benchmark/soar/demo_sala/preprocess_model.py)
- 上游：[openbmb/MiniCPM-SALA discussions/10](https://huggingface.co/openbmb/MiniCPM-SALA/discussions/10) — commit `f28de5e488b065a06bec8526d9683c14fe83bf7b`
- 上下文：[docs/soar_2026_changes/CHANGE_0151_phase_b_four_over_six_002.zh.md](../CHANGE_0151_phase_b_four_over_six_002.zh.md)（iter-3 abort，等本补丁落地后再跑 iter-4）
