# CHAT — Apply OpenBMB PR #10 `_init_rope` fix (transformers 5.x compat)

Session: 2026-05-08 12:18

## 1. User asks / agent actions

- **User**: "before go iter-4, pls help to patch modeling_minicpm_sala.py as the
  below public official announcement mentioned" (HF discussion #10).
- **Agent**: Fetched HF discussion #10 + commit `f28de5e4` raw diff. Confirmed
  scope: two `_init_rope()` methods, treat `None`/`"default"` as no-scaling,
  accept both `"type"` and `"rope_type"` keys, hoist `rope_scaling` to local var.
- **Agent**: Located the modeling file. It is **not** in our repo — it is
  shipped via HuggingFace `trust_remote_code=True` and lives only in the model
  directory. We already had an in-memory pre-load hack
  (`_install_gptqmodel_minicpm_rope_patch`) for the GPTQ flow but no protection
  for the sglang serving path.
- **Agent**: Decided to apply the upstream patch *in-place* on the model
  directory inside `preprocess_model.py` so it covers both quant time and
  runtime serving. Added `_patch_modeling_init_rope_inplace(model_dir, label)`
  with a marker-based idempotency check, plus call sites after each of the
  three modes (`gptq`, `nvfp4`, `copy`).
- **Agent**: Wrote a round-trip test that synthesizes a pre-fix file from the
  upstream post-fix file (by reversing the substitutions in the correct order),
  runs the patcher on it, and compares to the upstream post-fix byte-for-byte.
  Result: EXACT MATCH and idempotent on the already-patched file.
- **Agent**: Authored bilingual docs `CHANGE_0152_init_rope_transformers5_compat.{en,zh}.md`.

## 2. Outcomes

- New helper `_patch_modeling_init_rope_inplace` in
  `benchmark/soar/demo_sala/preprocess_model.py`, wired into all three modes.
- Local round-trip patch test passes (exact-match against upstream + idempotent).
- Docs CHANGE_0152 EN/ZH committed.

## 3. Open questions / follow-ups

- Iter-4 fcloud run will be the first end-to-end validation. Expect the
  `[preprocess][init-rope-patch]` log line to confirm 2 headers + 2 else-branches
  patched in the `dst` model dir.
- If any code path bypasses `preprocess_model.py` (unlikely for our flow),
  we may need to also patch the HF cache copy under
  `~/.cache/huggingface/modules/transformers_modules/...`. Defer until needed.

## 4. Cross-references

- Docs: [docs/soar_2026_changes/CHANGE_0152_init_rope_transformers5_compat.en.md](../CHANGE_0152_init_rope_transformers5_compat.en.md), [docs/soar_2026_changes/CHANGE_0152_init_rope_transformers5_compat.zh.md](../CHANGE_0152_init_rope_transformers5_compat.zh.md)
- Files modified: [benchmark/soar/demo_sala/preprocess_model.py](../../../benchmark/soar/demo_sala/preprocess_model.py)
- Upstream: [openbmb/MiniCPM-SALA discussions/10](https://huggingface.co/openbmb/MiniCPM-SALA/discussions/10) — commit `f28de5e488b065a06bec8526d9683c14fe83bf7b`
- Prior context: [docs/soar_2026_changes/CHANGE_0151_phase_b_four_over_six_002.en.md](../CHANGE_0151_phase_b_four_over_six_002.en.md) (iter-3 abort, deferring iter-4 until this patch lands)
